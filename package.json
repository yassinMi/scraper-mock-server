{
  "name": "scraper-mock-server",
  "version": "1.0.0",
  "description": "Creates a mocked web server exposing data and APIs that can be easily configured, useful to set up test envirements for e.g. scraping tools",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "yassinMi 2024",
  "license": "MIT",
  "dependencies": {
    "commander": "^11.1.0",
    "console-control-strings": "^1.1.0",
    "express": "^4.18.2"
  }
}
